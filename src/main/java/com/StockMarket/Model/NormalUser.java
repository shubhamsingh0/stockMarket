package com.StockMarket.Model;

import com.StockMarket.Entity.User;

public class NormalUser{

	/*private int userId;private String username;private String password; private String userType;private String email; long mobileNumber;
	private int statusConfirmed;
	
	public NormalUser(int userId, String username, String password, String userType, String email, long mobileNumber,
			int statusConfirmed) {
		super(userId, username, password, userType, email, mobileNumber, statusConfirmed);
		
		userId=super.getUserId();
		username=super.getUsername();
		password=super.getPassword();
		userType=super.getUserType();
		email=super.getEmail();
		mobileNumber=super.getMobileNumber();
		statusConfirmed=super.getStatusConfirmed();
		
		
	}
	public NormalUser(int userId,String username,String userType,String email,long mobileNumber)
	{
		userId=super.getUserId();
		username=super.getUsername();
		userType=super.getUserType();
		email=super.getEmail();
		mobileNumber=super.getMobileNumber();
		
		super(userId, username,userType, email, mobileNumber);
	}
	@Override
	public String toString() {
		return "NormalUser [userId=" + userId + ", username=" + username + ", password=" + password + ", userType="
				+ userType + ", email=" + email + ", mobileNumber=" + mobileNumber + ", statusConfirmed="
				+ statusConfirmed + "]";
	};*/
}
